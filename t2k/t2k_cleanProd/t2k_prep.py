#! /usr/bin/env python

import os
import sys
sys.path.append('../../libFns')



def grep_num( numb_list,  grep_list, file_list ):

    f_numb = open(numb_list, "r" )
    f_grep = open(grep_list, "r" )
    f_file = open(file_list, "w+")

    for line in f_numb



    # could do this in python, but so easy in bash..
    # while using txt files is no reason to do in python

    command_comm = 'comm -23 ' + anal_list +' '+ numc_list + ' > ' + 
    os.system(








